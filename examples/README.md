Mirror Collection Examples
==========================

```sh
git clone https://github.com/beyond-labs/react-mirror-collection.git
cd react-mirror-collection
npm install
cd examples
yarn install
yarn start
```

Then open `http://localhost:9000`.
